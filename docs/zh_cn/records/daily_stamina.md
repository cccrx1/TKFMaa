# 消耗体力（DailyStamina）验证记录

本文件记录消耗体力（DailyStamina）的实机验证、调试与覆盖状态，入口节点 `DailyStaminaStart`；流程、识别依据和限制见 [消耗体力（DailyStamina）](../tasks/daily_stamina.md)。

## 覆盖状态

- 主流程：已实现，详细流程以当前 Pipeline 为准。
- 高风险路径：以任务选项和对应流程说明为准，未明确覆盖的分支保持待验证。
- 自动通关的覆盖状态与复刻扩展待办见[自动战斗验证记录](auto_battle.md)。
- 已知待复核风险：扫荡弹窗较晚出现 Loading 时可能提前回退；扫荡开始后的未知网络/活动结束弹窗
  只能安全停止，尚无经过实机采集的专用处理；复刻地图边界仍依赖 `max_hit`；活动选择页只覆盖当前可见槽位。

## 验证记录

- 2026-09-13 实机故障注入：将活动与关卡目标替换为不存在文本，成功进入出征和限时活动页，未匹配目标时安全返回主界面；未消耗体力、药水或货币。
- 2026-09-13 实机状态核对：默认目标“刀剑魔舞－觉醒-01”对应活动页显示 `TIME'S UP`（活动已结束），流程在活动页停止，未消耗体力或恢复资源。默认活动配置需待下一次活动更新后重新采集验证。

### 2026-09-13 晚间入口与低体力返回复核

- 客户端 2.3.0、MuMuPlayer v5+（ADB `127.0.0.1:16384`）、720×1280、MaaFramework 5.13.0，
  MaaMCP 使用 EmulatorExtras 截图。测试副本保留目标节点的识别、点击和前后稳定等待，
  仅将后继改为优先检查 `CommonWaitLoading`、再确认目标页面的无动作观察节点。
- `DailyStaminaOpenExpeditionFromMain`：任务 `200000010` 总耗时约 6.303 秒，点击前等待 318 毫秒、
  点击后等待 4,348 毫秒，均成功；随后识别到限时活动入口。本次未复现历史上的 20 秒超时。
  更早一次探针被主界面礼包阻挡，目标节点未执行，不计入该结果。
- 当晚实际活动页为“刀剑魔舞”，挑战期限显示 `2026/09/30 10:00 (UTC+8)`，关卡列表显示剩余 16 天，
  可以进入磨刀-01 详情。这与上方早先 `TIME'S UP` 记录不同；此处保留两次观察，不推测差异原因，
  也不据此宣称整个活动路线或扫荡已经验证通过。
- `DailyStaminaReturnMainWhenLow`：使用 Interface 的“刀剑魔舞 - 磨刀-01”低体力识别 Override
  （消耗 30、预留 0），启动项目 Agent。实际详情页消耗为 30，Agent 识别体力 `28/150`，
  `DailyStaminaCompare` 返回 `stamina=28, cost=30, reserve=0, mode=low, hit=true`。
- 首次任务 `200000032` 点击后等待 20,024 毫秒超时。探针误用关卡列表作为返回后的观察条件，
  导致总耗时约 31.066 秒后判失败；重新截图确认实际已直接回到主界面。此失败不能归因于返回动作。
- 修正探针为 `CommonMainReady` 后复测，任务 `200000040` 总耗时约 22.581 秒；点击前等待 320 毫秒成功，
  点击后全屏等待 20,025 毫秒失败，随后成功确认主界面。两次均复现全屏稳定等待超时，生产节点尚未修改。
- 全程未点击开始或军团扫荡，未使用体力恢复资源；末次主界面仍为体力 `28/150`，货币 `2,559,474`、
  魔晶石 `74,780`、第三项资源 `87`。本轮仅覆盖入口和真实低体力返回，未覆盖扫荡或恢复流程。

### 2026-09-13 深夜入口超时复核

- 同上环境重新运行完整 `DailyStaminaStart`（任务 `200000017`，约 24.375 秒）：入口
  `DailyStaminaOpenExpeditionFromMain` 的前置稳定等待 318 毫秒、动作后为其他节点耗时，
  本次日志中该任务 0 次 `Wait freezes timeout`，未复现上一节的 20,025 毫秒失败。
  当晚默认活动页显示 `TIME'S UP`，流程安全停在活动页并返回主界面，未消耗体力。
- 公共加载识别（`CommonWaitLoading`）的放宽只影响 `next` 判断时机，不能消除 `wait_freezes` 的 20 秒上限；
  证据与隔离实验见 [公共主界面恢复验证记录](common_navigation.md)。

### 2026-09-15 导航路线回归

- 客户端当前显示版本 `v060`，MuMuPlayer v5+，720×1280，MaaMCP ADB 连接；使用测试副本加载完整体力相关文件，
  通过 Interface 的“常驻关卡 - 战争演习-特IV” Override，实际走通“主界面 → 目前活动 → 日常政务 → 战争演习 →
  关卡详情”。任务约 11.7 秒结束，详情页识别到 `战争演习-特IV`、`军团扫荡`、`开始` 和 `150/150`。
- 同一测试副本将关卡详情决策替换为立即停止，未点击扫荡、开始、药水或购买按钮；体力保持 `150/150`。
- 使用“刀剑魔舞 - 磨刀-01” Override 回归常规活动分页路线，实际走通“主界面 → 限时活动 → 活动卡 → 分页列表回退到第一页 →
  目标关卡 → 关卡详情”。任务约 16.9 秒结束，详情页 OCR 命中 `磨刀-01` 和 `军团扫荡`，未产生资源消耗。
- 两条路线均未出现 `WaitFreezes` 超时。返回主界面节点单独测试约 7.1 秒完成，经过 `CommonStopOnMainWithRetries`、
  `CommonStopOnMainObserve`、`CommonStopOnMainConfirm` 和 `CommonStopOnMain` 确认结束。
- 本轮只覆盖日常政务普通列表与常规活动分页列表；复刻地图、大型活动特殊 UI、扫荡动画、手动战斗、自动战斗和真实网络缓冲仍待验证。
