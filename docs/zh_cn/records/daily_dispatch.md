# 每日派遣（DailyDispatch）验证记录

本文件记录每日派遣（DailyDispatch）的实机验证、调试与覆盖状态，入口节点 `DailyDispatchStart`；流程、识别依据和限制见 [每日派遣（DailyDispatch）](../tasks/daily_dispatch.md)。

## 覆盖状态

- 闲置队伍的选点、选时与出发子流程尚待实机验证。
- 完成队伍归来、闲置队伍出发、队伍未就绪、各地点和时间选项及超时恢复仍待验证；
  本轮未加载 Interface Case Override，不将默认分支测试算作选项覆盖。

## 验证记录

- 打开抽屉、从倒计时栏位进入总览和返回主界面时，当前动画会令原全屏静止等待各超时约 20 秒。
- 2026-09-13，客户端 2.3.0，MuMuPlayer v5+，MaaMCP 原生截图与输入：三个队伍均忙碌，
  完整入口覆盖打开抽屉、按倒计时进入地城派遣总览、识别忙碌状态并返回确认主界面。
- 修复前约 66.9 秒，三处全屏等待均触发约 20 秒超时；重新连接并加载修复后约 7.3 秒，
  无稳定等待或动作失败。未取消派遣、改变队伍或派出新队伍。

### 2026-09-13 晚间其余三个交互节点复核

- 客户端 2.3.0、MuMuPlayer v5+（ADB `127.0.0.1:16384`）、720×1280、MaaFramework 5.13.0，
  MaaMCP 使用 EmulatorExtras 截图。三个队伍均在派遣中，最短剩余时间仍约 12 小时。
- `DailyDispatchClickActionableDrawerSlot` 的测试保留原识别和动作，优先判断 `CommonWaitLoading`。
  任务 `200000019` 约 8.687 秒后未命中目标退出，没有执行点击及稳定等待，不能计为该动作验证通过。
- 手动从倒计时栏位进入总览后，用原识别条件的只读反向检查确认 `DailyDispatchCloseReturnPopup`
  和 `DailyDispatchLaunchReady` 均不命中当前忙碌页面；任务 `200000021` 约 1.516 秒通过。
- 可操作抽屉栏位、归来弹窗关闭、队伍就绪出发的正向路径及其稳定等待仍待验证。
  本轮未取消、加速或发起派遣，没有为测试改变队伍状态。
