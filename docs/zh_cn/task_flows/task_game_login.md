# 启动游戏（GameLogin）

入口节点：`GameLoginStart`；实现文件：`assets/resource/pipeline/game_login.json`。

## 前置状态

- 模拟器已安装所选服务器对应的游戏包，并允许从主界面启动应用。
- 在任务选项“选择服务器”中确认包名：工口服务器为 `com.pinkcore.tkfm`，erolab 服务器为 `com.pinkcore.tkfm.erolabs`。

## 主流程

1. 关闭已运行的游戏应用，再启动所选服务器的游戏包。
2. 等待启动加载，按出现顺序关闭公告。
3. 识别标题页并点击进入游戏；点击未生效时有限次重试。
4. 关闭登录后的活动签到，常规签到，公告和礼包弹窗，直至识别到主界面。

## 页面识别

| 页面状态  | 识别目标                                | 识别方式  | ROI/素材                 |
| --------- | --------------------------------------- | --------- | ------------------------ |
| 启动加载  | `LOADING`/`LOAD ING`                    | OCR       | 通用 `CommonWaitLoading` |
| 启动公告  | 王城公布栏、活动公告或最新公告          | OCR       | 公告标题区域             |
| 标题页    | `TAP TO START`、`TOUCH SCREEN` 或版本号 | Or + OCR  | 标题页底部               |
| 签到/礼包 | 签到文本、`STEP`、限时、精选或月卡      | OCR       | 弹窗内容区               |
| 主界面    | 出征与调教同时存在                      | And + OCR | `CommonMainReady`        |

## 异常与分支

- 公告、签到和礼包均为可选分支，未出现时直接继续判断下一状态。
- 多层礼包会循环关闭；标题页点击以 `max_hit` 限制重试次数。
- 网络错误、服务器维护或资源更新页面目前没有专用恢复节点，出现时应安全停止并补充流程记录。

## 完成状态

- 成功条件：`GameMainReady` 命中并停止任务。
- 安全退出位置：游戏主界面；无法识别已知页面时不得盲点继续。

## 验证记录

- 2026-07-14：根据导入的历史说明和当前 Pipeline 整理，未重新运行模拟器。
- 待验证：两个服务器包名、公告/签到/礼包的不同出现顺序，以及网络异常页面。
